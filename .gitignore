.nicegui/

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/  

pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt  


# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/
docs/source/conf.py

# PyBuilder
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
.python-version

# dotenv
.env

# virtualenv
.venv
venv/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy  

.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/  

# Docker
.docker/
docker-compose.override.yml

# **Explanation of Common Entries:**

# * **`__pycache__/`:**  Stores compiled Python bytecode files.
# * **`*.py[cod]`:** Matches any files ending with `.pyc`, `.pyo`, or `.pyd` (compiled Python files).
# * **`*.so`:**  Shared object files (compiled C extensions).
# * **`build/`, `dist/`, `eggs/`, etc.:**  Directories related to packaging and distribution.
# * **`*.egg-info/`:** Metadata about installed packages.
# * **`*.log`:** Log files generated by your application.
# * **`local_settings.py`:** Often used to store sensitive settings in Django projects.
# * **`db.sqlite3`:** Default SQLite database file for Django.
# * **`instance/`:**  Flask's instance folder for local configuration.
# * **`htmlcov/`, `.tox/`, `.coverage`, etc.:**  Directories and files related to testing and code coverage.
# * **`*.mo`, `*.pot`:**  Translation files.
# * **`.env`:**  Files containing environment variables (often used with the `python-dotenv` package).
# * **`.venv`, `venv/`:**  Virtual environment directories.
# * **`.idea/`:**  PyCharm project settings. (Often included, but you might want to track this if you have specific settings to share)
