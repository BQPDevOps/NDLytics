grid_card_animation = """  <style>
        .animate-card {
            animation: fadeInDrop 0.5s ease-out forwards;
        }

        @keyframes fadeInDrop {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        </style>
        """
